const chai = require('chai');
const expect = chai.expect;

const accessLogger = require('./access-log');

describe('Access log middleware', function() {
  it('should log to winston logger', function() {
    
  });
});

'use strict';

/**
 * Creates a new post
 * @param {Object} parent Parent type
 * @param {Object} args Arguments passed from query
 */
function createPost(parent, args) {
  return Promise.resolve();
}

module.exports = {
  createPost: createPost
};

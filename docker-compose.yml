version: '2'
services:
  kraken:
    container_name: 'kraken'
    build: .
    environment:
      PORT: 8000
      ENDPOINT: '/graphql'
      NODE_ENV: 'development'
      LOG_DIR: '/var/log/kraken'
    volumes:
      - ./:/home/node/app
    ports:
      - 80:8000
